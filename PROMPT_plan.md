# ScentScape — Planning Mode

0a. Study `specs/*` with up to 250 parallel Sonnet subagents to learn the application specifications for UI/UX redesign, quiz personality engine, and monetization strategy.
0b. Study @IMPLEMENTATION_PLAN.md (if present).
0c. Study `apps/web/lib/*` and `apps/web/components/*` with up to 250 parallel Sonnet subagents to understand existing implementations.
0d. For reference, the application source code is in `apps/web/` (Next.js frontend) and `apps/api/` (Medusa backend).

1. Study @IMPLEMENTATION_PLAN.md and use up to 500 Sonnet subagents to study existing source code in `apps/web/` and `apps/api/` and compare against `specs/*`. Use an Opus subagent to analyze findings, prioritize tasks, and create/update @IMPLEMENTATION_PLAN.md as a bullet point list sorted in priority of items yet to be implemented. Ultrathink.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing — search the codebase first.

ULTIMATE GOAL: Transform ScentScape from a bland, developer-built MVP into a premium fragrance discovery platform. The quiz is the hero product — it must feel like a deep personality assessment with non-deterministic, AI-aware recommendations. The UI must evoke sensory richness. Everything should guide users from "I know nothing about fragrance" to "I found my signature scent." The quiz remains free — no paywall. Revenue comes from affiliate commerce, sample boxes, and premium experiences layered on top.

KEY PRIORITIES (in order):
1. Quiz personality engine — deep, branching, non-deterministic, AI-ready
2. Visual identity and design system — premium, sensory, editorial feel
3. Homepage transformation — immersive first impression, clear value prop
4. Product discovery UX — educational, explorable, not just filterable
5. Mobile-first experience — native-app quality on phones
6. Monetization architecture — affiliate links, sample kits, premium tier foundations
7. Educational content layer — contextual learning woven into every surface
